/** Hosted product origin (login, signup, and the app itself). */
export const APP_ORIGIN = 'https://app.headquarters-crm.com';

export const appLoginUrl = `${APP_ORIGIN}/login`;
export const appSignupUrl = `${APP_ORIGIN}/signup`;
